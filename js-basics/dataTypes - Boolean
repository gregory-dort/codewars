function trueOrFalse(val){
  if (val == false || val == null || val == undefined)
    return "false";
  else
    return "true";
}

// Implicitly defined null and undefined values were false.
// 0, NaN, empty strings are also false.
// Everything else considered true.