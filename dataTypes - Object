function animal(obj){
  return `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;
}

// The object was already defined on codewars with the color, name and legs properties.
// You can reference those by calling obj.{property name} and return the values.